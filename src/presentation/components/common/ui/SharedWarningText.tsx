import styled from '@emotion/styled';

const SharedWarningText = styled.p`
  color: #d9534f; // Standard danger/warning color
  font-size: 0.75rem; // 12px
  margin-top: 0.5rem; // 8px
  // Add other common warning text styles if needed, e.g., text-align, font-style
`;

export default SharedWarningText; 